<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin_Inprocess</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="/stylesheet/admin_inprocess.css">
</head>
<body>

    <header>
        Mess Menia
        <div class="menu-icon" onclick="toggleMenu()">☰</div>
    </header>

    <div class="container">
        <div class="account-info" id="account-info">
            <!-- ... (account info content) ... -->
            <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/2/2e/Microsoft_Account_Logo.svg/1200px-Microsoft_Account_Logo.svg.png" alt="Logo">
            <h3>Welcome, <%= name %></h3>
            <nav>
                        <ul>
                            <li><a href="/admin_dashboard">Dashboard</a></li>
                            <li><a href="/admin_menu">Mess Menu</a></li>
                            <li><a href="/admin_inprocess">Mess Complaint</a></li>
                            <li><a href="/admin_chat">Chat</a></li>
                            <!-- Add more menu items as needed -->
                        </ul>
                        
            </nav>
            <form class="footer" method="post" action="/logout">
                <button type="submit">Logout</button>
            </form>
        </div>

        <div class="complaint-section">
            <!-- Your complaint section goes here -->
            <h2>Mess Complaints</h2>
            <div class="complaint-links">
                <a href="/admin_inprocess" class="active">In Process</a>
                <a href="/admin_resolve">Resolved</a>
            </div>
            <hr>
            <!-- In Progress Section -->
            <div class="progress-section">
                <!-- Progress Box 1 -->
                <%if(record.length > 0) { %>
                    <% record.forEach(function(element){ %>
                        <div class="progress-box">
                            <h3><%= element.title %></h3>
                            <p><%= element.description %></p>
                            <form action="/admin_inprocess" method="post">
                            <input type="hidden" name="title" value="<%= element.title %>" >
                            <input type="hidden" name="description" value="<%= element.description %>" >
                            <button type="submit" class="resolve-button">Resolve</button>
                        </form>
                        </div>
                    <% }) %>
                    <% } %>
            </div>
        </div>
    </div>

    <script src="/javascript/responsive.js"></script>

</body>
</html>